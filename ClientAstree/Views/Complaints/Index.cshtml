@model IEnumerable<ClientAstree.Models.ComplaintVM>



<h2>All Complaints</h2>
<table class="table">
    <thead>
        <tr>
            <th>Subject</th>
            <th>Description</th>
            <th>Status</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.ComplaintsSubject</td>
            <td>@item.Description</td>
            <td>@item.ComplaintState</td>
            <td>
                <a href="@Url.Action("Edit", "Complaints", new { id = item.Id })" class="btn btn-primary">Edit</a>
                <a href="@Url.Action("Delete", "Complaints", new { id = item.Id })" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this?');">Delete</a>
            </td>
        </tr>
    }
    </tbody>
</table>
